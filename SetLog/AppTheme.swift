import SwiftUI

enum AppTheme {
    static let pageBackground = Color(uiColor: .systemGroupedBackground)
    static let cardBackground = Color(uiColor: .secondarySystemGroupedBackground)
    static let subtleFill = Color(uiColor: .tertiarySystemFill)
    static let cardBorder = Color(uiColor: .separator).opacity(0.22)
    static let strong = Color(uiColor: .label)
    static let invertedStrong = Color(uiColor: .systemBackground)
    static let accent = Color.orange
}
