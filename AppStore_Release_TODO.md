# SetLog App Store Release ToDo

## P0 上架阻塞
- [x] 清理主要无动作入口（模板页顶部按钮、历史页顶部按钮、历史详情右上角按钮、首页模板卡空菜单按钮、个人页顶部按钮）。
- [x] 模板应用流程闭环：从首页/模板页应用真实 `WorkoutTemplate` 启动训练。
- [x] 历史详情「复制为新训练」可用：根据历史记录复制动作与组数并进入训练。
- [x] 示例数据注入仅限 Debug（Release 不再自动注入演示数据）。
- [x] 本地数据库重置兜底仅限 Debug（Release 不再遇到模型兼容问题即删库）。
- [ ] 模板管理闭环（创建/编辑/删除模板）仍缺失，当前仅有“模板创建入口”占位卡片。
- [ ] 设置页导航项闭环（显示模式、登录与同步、帮助中心、关于）需接真实页面或改为纯说明样式。
- [ ] 数据迁移与启动容错策略补齐（当前 ModelContainer 初始化失败仍会 `fatalError` 终止）。
- [ ] 补齐 App Icon 最终资源（当前 iconset 结构在，但未确认最终图稿文件）。
- [ ] 完成 App Store Connect 必填元数据（副标题、描述、关键词、支持 URL、营销 URL、版本说明等）。
- [ ] 完成隐私问卷（App Privacy）与隐私政策 URL。
- [ ] 完成隐私清单（Privacy Manifest）与 Required Reason APIs 声明核查。
- [ ] 完成年龄分级新问卷（2026 要求）。
- [ ] 完成出口合规（Encryption）声明与必要配置。
- [ ] 完成 App Review 信息（审核联系人、审核备注、必要时演示账号与步骤）。
- [ ] 提交流程门禁（Archive -> Validate -> TestFlight 冒烟 -> 正式提交）形成固定 checklist。

## P1 高优先
- [x] 历史页筛选按钮接入真实排序能力（按时间/按容量）。
- [x] 清理当前编译 warning（`AddExerciseView.swift` 未使用绑定值）。
- [ ] 通知开关做成真实功能（权限请求+提醒调度）或在首发隐藏。
- [ ] 增强错误处理：替换关键链路 `try?` 静默失败为可追踪错误提示。
- [ ] 账号状态来源真实化（当前 `accountState` 固定为 `.guest`，已登录路径不可达）。
- [ ] 训练中页面关键标签去硬编码（如“胸部训练 / 力量模式”应跟随当前 session 数据）。
- [ ] 增加核心流程测试（训练中编辑/完成、模板应用、历史复刻、设置持久化）。

## P2 体验优化
- [ ] 多语言一致性（中英文案统一策略）。
- [ ] 可访问性（动态字体、VoiceOver 标签、对比度检查）。
- [ ] 数据导出/备份策略（本机模式建议支持导出）。

## 发布配置检查
- [ ] 确认最低系统版本策略（当前 `IPHONEOS_DEPLOYMENT_TARGET = 26.2`）。
- [ ] 确认 iPad 是否首发支持（影响截图与适配工作量）。
